{
  "$comment": "Worker role. Full access bounded by CLAUDE.md + policy hooks. No paseo MCP. Write/edit/commit/push authority is derived from the CURRENT-turn V3 brief; malformed hook state blocks the turn. Shell is disabled on read-only turns, and direct file mutations are confined to OWNED_SCOPE. Native Agent/Task tools + web denied.",
  "permissions": {
    "defaultMode": "bypassPermissions",
    "deny": ["Agent", "WebSearch", "WebFetch"]
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "(Bash|PowerShell|Edit|Write|MultiEdit|NotebookEdit|Artifact|Agent|Task|mcp__paseo__.*)",
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_CONFIG_DIR}/hooks/pre-tool-use.mjs\""
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_CONFIG_DIR}/hooks/user-prompt-submit.mjs\""
          }
        ]
      }
    ]
  }
}
