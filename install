#!/usr/bin/env bash
# POSIX convenience wrapper. The cross-platform dispatcher lives in install.mjs.
set -euo pipefail
script_dir="$(cd "$(dirname "${BASH_SOURCE[0]:-$0}")" && pwd)"
exec node "$script_dir/install.mjs" "$@"
