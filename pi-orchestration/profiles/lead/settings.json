{
  "$comment": "Lead role resources. pi-mcp-adapter is shared via the symlinked npm/ dir; it provides the `mcp` proxy tool that reaches the Paseo MCP server configured in mcp.json. The policy extension enforces per-role tool allowlists. defaultProjectTrust=always loads project resources by default, including project-scoped .agents/skills.",
  "extensions": ["./extensions/paseo-team-policy.ts"],
  "skills": ["./skills"],
  "prompts": ["./prompts"],
  "defaultProjectTrust": "always",
  "enableSkillCommands": true,
  "packages": [
    "npm:pi-mcp-adapter",
    "npm:@juicesharp/rpiv-ask-user-question",
    "git:github.com/vanhiep99w/pi-learn"
  ]
}
