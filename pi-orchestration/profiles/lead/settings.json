{
  "$comment": "Lead role resources. Orchestration uses the role-gated paseo-team CLI facade; the policy extension enforces per-role tool allowlists. defaultProjectTrust=never keeps role behavior deterministic (project AGENTS.md context still loads); set to \"always\" to also load project .pi resources.",
  "extensions": ["./extensions/paseo-team-policy.ts"],
  "skills": ["./skills"],
  "prompts": ["./prompts"],
  "defaultProjectTrust": "never",
  "enableSkillCommands": true,
  "packages": [
    "npm:@juicesharp/rpiv-ask-user-question",
    "git:github.com/vanhiep99w/pi-learn"
  ]
}
