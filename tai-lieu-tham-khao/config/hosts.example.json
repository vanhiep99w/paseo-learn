{
  "$comment": "COPY → ~/.paseo-pi-team/hosts.local.json for N-host setups. Never commit the local copy. Endpoint VALUES (tcp://host:port?ssl=true&password=... or pairing offer URLs) live in environment variables referenced by NAME via 'endpointEnv' — never inside this file. hostId must match model-routing.local.json hostId of the same machine. The special hostId 'local' means: the daemon this Lead talks to through the injected MCP server (no env needed).",
  "version": 1,
  "hosts": {
    "local": {
      "capabilities": ["git-read", "git-write", "focused-test", "independent-review"],
      "limits": {
        "writers": 1,
        "readers": 2
      }
    },
    "host-example": {
      "endpointEnv": "PASEO_HOST_EXAMPLE",
      "capabilities": ["git-read", "independent-review"],
      "limits": {
        "writers": 0,
        "readers": 2
      }
    }
  }
}
